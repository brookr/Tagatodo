%h1 Things To Do

= link_to 'New Todo', new_todo_path, :'data-pjax' => '#content'

- @todos.each do |todo|
  %ul
    %li
      = link_to todo.title, todo, :'data-pjax' => '#content',
                                  :class => "#{ 'completed' if todo.completed }"
